# Ablation A4: Full system minus CrossCLR
# Tests whether contrastive alignment contributes

inherits: ../phase3.yaml

losses:
  ce_weight: 1.0
  diversity_weight: 0.2
  terminology_weight: 0.4
  crossclr_weight: 0.0
  ewc_lambda: 400